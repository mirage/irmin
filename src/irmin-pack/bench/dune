(library
  (name rusage)
  (modules rusage)
  (c_names rusage_stubs))

(executable
 (name pack_bench)
 (modules pack_bench)
 (libraries rusage irmin_pack bos logs.fmt cmdliner fmt.cli fmt.tty logs.cli))

(alias
 (name bench)
 (action
  (run ./pack_bench.exe)))
