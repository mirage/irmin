language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script: bash -ex .travis-opam.sh
sudo: required
env:
  global:
  - ALCOTEST_SHOW_ERRORS=1
  - REVDEPS="irmin-indexeddb"
  - PACKAGE=irmin
  - PINS="irmin-indexeddb:https://github.com/samoht/irmin-indexeddb.git"
  - EXTRA_REMOTES="https://github.com/mirage/mirage-dev.git"
  matrix:
  - OCAML_VERSION=4.02 TESTS=false
  - OCAML_VERSION=4.03 TESTS=true EXTRA_DEPS=inotify
  - OCAML_VERSION=4.03 DEPOPTS=git TESTS=false
  - OCAML_VERSION=4.03 DEPOPTS=git-mirage TESTS=false
  - OCAML_VERSION=4.04 DEPOPTS=cohttp TESTS=true
  - OCAML_VERSION=4.04 DEPOPTS="cohttp git" TESTS=false
